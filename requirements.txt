# Core data manipulation and analysis
pandas>=2.2.0              # For working with tabular data (CSV files) - Compatible with Python 3.14
numpy>=1.26.0              # For numerical operations and arrays

# Machine Learning and NLP
scikit-learn>=1.3.0        # For clustering algorithms (K-Means, DBSCAN) and preprocessing
openai>=1.0.0              # For OpenAI API (letter generation) - RECOMMENDED
google-generativeai>=0.3.0 # For Gemini API (alternative)
transformers>=4.36.0       # For pre-trained NLP models (emotion detection only)

# Sentiment Analysis
nltk>=3.8                  # Natural Language Toolkit for text processing

# Visualization
matplotlib>=3.8.0          # For creating plots and visualizations
seaborn>=0.13.0            # For statistical visualizations
plotly>=5.18.0             # For interactive visualizations

# Image Processing (for album covers)
Pillow>=10.1.0             # For image manipulation
colorthief>=0.2.1          # For extracting dominant colors from images

# Web Interface (FastAPI backend for the React frontend)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
gradio>=4.12.0             # legacy Gradio app (app.py); optional

# API (optional - for Spotify integration if needed later)
spotipy>=2.23.0            # Spotify API wrapper

# Utilities
tqdm>=4.66.0               # For progress bars
python-dotenv>=1.0.0       # For managing environment variables
joblib>=1.3.0              # For saving/loading models
wordninja>=2.0.0           # Splits glued lowercase titles e.g. "thelastgreatamericandynasty"
